server:
  port: 8080

spring:
  application:
    name: user
eureka:
  instance:
    hostname: user
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://peer1:8761/eureka/,http://peer2:8762/eureka/
